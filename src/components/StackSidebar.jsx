import React from "react";export default function StackSidebar({ stack, onRemove, onRemoveAll }) { const items = Object.values(stack); const count = items.length; return (<div className="border border-gray-200 rounded-2xl p-5 bg-white h-fit lg:sticky lg:top-24"><h3 className="font-bold mb-1">Your Stack</h3><p className="text-xs text-gray-400 mb-4">{count} Technology Selected</p>{count === 0 ? (<p className="text-sm text-gray-400 mb-4">No technologies selected yet. Pick one from each category to start building your stack.</p>) : (<div className="grid grid-cols-1 gap-3 mb-4">{items.map((tech) => (<div key={tech.id} className="flex items-center justify-between border border-gray-100 rounded-xl px-3 py-2"><div className="flex items-center gap-2 min-w-0"><img src={tech.icon} alt={`${tech.name} logo`} className="w-6 h-6 object-contain flex-shrink-0" onError={(e) => { e.currentTarget.style.display = "none"; }} /><div className="min-w-0"><p className="text-sm font-semibold leading-tight truncate">{tech.name}</p><p className="text-xs text-gray-400 leading-tight">{tech.category}</p></div></div><button onClick={() => onRemove(tech)} className="text-gray-400 hover:text-red-500 text-lg leading-none flex-shrink-0 pl-2" aria-label={`Remove ${tech.name}`}>✕</button></div>))}</div>)}<button onClick={onRemoveAll} disabled={count === 0} className="w-full border border-red-200 text-red-500 font-semibold text-sm py-2.5 rounded-lg hover:bg-red-50 transition disabled:opacity-40 disabled:cursor-not-allowed">Remove All</button></div>); }